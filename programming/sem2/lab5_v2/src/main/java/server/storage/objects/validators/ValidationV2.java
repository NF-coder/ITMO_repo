package server.storage.objects.validators;

import server.storage.objects.exceptions.UnacceptableValue;

public class ValidationV2 {
    public <T> void validate(String argName, T value) throws UnacceptableValue {
        validateArg
    }
}
